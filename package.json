{
  "name": "metis-trail-v2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/build.mjs --watch",
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "firebase": "^12.14.0",
    "leaflet": "^1.9.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "vitest": "^2.0.0"
  }
}
